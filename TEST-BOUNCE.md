# 🎾 弹跳动画测试指南

## 🐛 问题描述

用户反馈：**弹跳动画没有对应的动画效果**

## ✅ 已修复

### 修复内容

1. **增加弹跳幅度**
   - 从 100px → 150px
   - 更明显的视觉效果

2. **优化数学表达式**
   - 使用 `abs(sin())` 函数创建弹跳曲线
   - 每个视频3次弹跳
   - 更流畅的动画过渡

3. **改进画布处理**
   - 更好的 padding 计算
   - 防止文字被裁剪

4. **添加调试信息**
   ```
   ✅ 弹跳效果 (上下弹跳)
   📊 弹跳参数: 高度=150px, 频率=3次
   ```

### 技术实现

```javascript
case 'bounce':
  const bounceHeight = 150;  // 弹跳高度
  const bounceFreq = 3;      // 弹跳次数
  
  command.videoFilters([
    // 1. 扩大画布，留出弹跳空间
    `pad=iw:ih+${bounceHeight}:0:${bounceHeight/2}`,
    
    // 2. Y坐标随时间上下移动
    // abs(sin()) 创建 0→1→0 的弹跳曲线
    `crop=iw:ih:0:${bounceHeight/2}+${bounceHeight/2}*abs(sin(${bounceFreq}*2*PI*t/${duration}))`
  ].join(','));
  break;
```

### 弹跳曲线解释

```
abs(sin(3*2*PI*t/duration))

时间 t=0:    sin(0) = 0        → Y位置: 中间
时间 t=1/6:  sin(π) = 0        → Y位置: 中间 (第1次弹跳)
时间 t=1/3:  sin(2π) = 0       → Y位置: 中间 (第2次弹跳)
时间 t=1/2:  sin(3π) = 0       → Y位置: 中间 (第3次弹跳)

峰值:        abs(sin(π/2)) = 1 → Y位置: 最高点
```

---

## 🧪 测试步骤

### 步骤1: 重启服务器

```bash
# 停止旧服务器
pkill -f "node.*server.js"

# 启动新服务器
npm start
```

### 步骤2: 访问并刷新

```
http://localhost:3000/
```

**硬刷新**:
- Windows/Linux: `Ctrl + Shift + R`
- macOS: `Cmd + Shift + R`

### 步骤3: 填写表单

1. **文本内容**:
   ```
   🎾 测试弹跳动画
   Bounce Test
   ```

2. **背景音乐**: 上传任意音频文件

3. **幻灯片时长**: 设置为 **5秒** 或更长

4. **文本动画效果**: 选择 **弹跳**

5. **其他设置**: 保持默认

### 步骤4: 生成视频

1. **点击"🚀 生成视频"**

2. **查看后台日志** - 应该显示:
   ```
   🎬 应用动画效果: bounce
     ✅ 弹跳效果 (上下弹跳)
     📊 弹跳参数: 高度=150px, 频率=3次
   ```

3. **等待生成完成**

4. **下载视频**

### 步骤5: 播放验证

**播放生成的视频，应该看到**:

1. ✅ 文字在屏幕上**上下弹跳**
2. ✅ 弹跳幅度**明显可见**（约150像素）
3. ✅ 5秒内弹跳**3次**
4. ✅ 弹跳**流畅**，无突兀感
5. ✅ 文字不被裁剪

---

## 📊 动画对比

### 弹跳动画的预期效果

```
时刻 0.0秒: 文字在中间位置
时刻 0.3秒: 文字向上移动 ↑
时刻 0.5秒: 文字到达最高点 ⬆️
时刻 0.7秒: 文字向下移动 ↓
时刻 1.0秒: 文字回到中间位置 (第1次弹跳完成)

时刻 1.3秒: 文字向上移动 ↑
时刻 1.5秒: 文字到达最高点 ⬆️
时刻 1.7秒: 文字向下移动 ↓
时刻 2.0秒: 文字回到中间位置 (第2次弹跳完成)

时刻 2.3秒: 文字向上移动 ↑
时刻 2.5秒: 文字到达最高点 ⬆️
时刻 2.7秒: 文字向下移动 ↓
时刻 3.0秒: 文字回到中间位置 (第3次弹跳完成)

时刻 3.0-5.0秒: 保持静止
```

### 对比其他动画

| 动画类型 | 效果描述 |
|---------|---------|
| 无动画 | 文字静止不动 |
| 淡入淡出 | 透明度变化 0% → 100% → 0% |
| 从右向左滑入 | 文字从右边滑入 ← |
| 从左向右滑入 | 文字从左边滑入 → |
| 从下向上滑入 | 文字从下边滑入 ↑ |
| 从上向下滑入 | 文字从上边滑入 ↓ |
| 缩放 | 文字从小放大 0.5x → 1x |
| 旋转 | 文字360度旋转 🔄 |
| **弹跳** | **文字上下弹跳 3次 ⬆️↓⬆️↓⬆️↓** |

---

## 🎨 弹跳参数调整

如果需要自定义弹跳效果，可以修改以下参数：

### 1. 弹跳高度
```javascript
const bounceHeight = 150;  // 增大 = 弹得更高
```

- `100` - 小幅弹跳
- `150` - 中等弹跳（默认）
- `200` - 大幅弹跳
- `250` - 超大弹跳

### 2. 弹跳频率
```javascript
const bounceFreq = 3;  // 增大 = 弹跳次数更多
```

- `1` - 1次弹跳
- `2` - 2次弹跳
- `3` - 3次弹跳（默认）
- `5` - 5次弹跳（快速）

### 3. 弹跳速度

通过调整 `duration` (视频时长) 控制：
- 5秒视频 + 3次弹跳 = 每1.67秒弹1次
- 10秒视频 + 3次弹跳 = 每3.33秒弹1次

---

## 🔍 调试方法

### 查看FFmpeg命令

后台日志会显示实际的FFmpeg滤镜：

```bash
# 应该看到类似输出：
🎬 应用动画效果: bounce
  ✅ 弹跳效果 (上下弹跳)
  📊 弹跳参数: 高度=150px, 频率=3次
🎥 FFmpeg滤镜: pad=1920:1230:0:75,crop=1920:1080:0:75+75*abs(sin(3*2*PI*t/5))
```

### 验证滤镜表达式

可以在命令行测试FFmpeg滤镜：

```bash
ffmpeg -i text_image.png -filter_complex "
  pad=1920:1230:0:75,
  crop=1920:1080:0:75+75*abs(sin(3*2*PI*t/5))
" -t 5 -r 30 test_bounce.mp4
```

### 检查生成的文本图像

```bash
ls -lh temp/text_image_*.png
```

用图像查看器打开，确认文字没有被裁剪。

---

## 🎬 视频示例描述

**正确的弹跳效果应该是：**

```
┌────────────────────────────────────┐
│                                    │
│           ⬆️ 文字上移              │  ← 0.5秒
│                                    │
│                                    │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│                                    │
│                                    │
│           文字在中间                │  ← 1.0秒
│                                    │
│                                    │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│                                    │
│                                    │
│                                    │
│           ⬇️ 文字下移              │  ← 此位置不会出现（abs处理）
│                                    │
└────────────────────────────────────┘

重复3次...
```

**注意**: 使用 `abs(sin())` 所以文字不会向下移动超过中心位置，只在上半部分弹跳。

---

## ⚠️ 常见问题

### Q1: 看不到弹跳效果？

**检查清单**:
- [ ] 确认选择了"弹跳"动画
- [ ] 视频时长至少3秒（推荐5秒以上）
- [ ] 后台日志显示"✅ 弹跳效果"
- [ ] 文字没有被背景图片遮挡

### Q2: 弹跳幅度太小？

**解决方法**:
```javascript
// 在 video-generator.js 中修改
const bounceHeight = 200;  // 从150增加到200
```

### Q3: 弹跳太快或太慢？

**调整频率**:
```javascript
const bounceFreq = 2;  // 从3改为2（更慢）
const bounceFreq = 5;  // 从3改为5（更快）
```

### Q4: 文字被裁剪？

**原因**: padding 不足

**解决方法**:
```javascript
// 增大 padding
`pad=iw:ih+${bounceHeight*1.5}:0:${bounceHeight*0.75}`
```

---

## 📝 对比测试

建议生成3个视频对比：

### 测试1: 无动画
- 动画: 无动画
- 时长: 5秒
- **预期**: 文字静止

### 测试2: 淡入淡出
- 动画: 淡入淡出
- 时长: 5秒
- **预期**: 透明度变化

### 测试3: 弹跳（本次修复）
- 动画: 弹跳
- 时长: 5秒
- **预期**: 文字上下弹跳3次

对比播放，弹跳效果应该非常明显。

---

## ✅ 验收标准

弹跳动画修复成功的标准：

- [ ] 后台日志显示"✅ 弹跳效果 (上下弹跳)"
- [ ] 后台日志显示"📊 弹跳参数: 高度=150px, 频率=3次"
- [ ] 生成的视频中文字有明显的上下移动
- [ ] 5秒视频内弹跳3次
- [ ] 弹跳流畅，无卡顿
- [ ] 文字不被裁剪
- [ ] 与其他动画效果明显不同

---

## 🚀 立即测试

```bash
# 1. 重启服务器
npm start

# 2. 访问浏览器
http://localhost:3000/

# 3. 填写表单
文本内容: 🎾 弹跳测试
动画效果: 弹跳
时长: 5秒

# 4. 生成并播放视频
# 5. 验证弹跳效果
```

---

**弹跳动画已修复！请测试并反馈效果！** 🎾
