# 🧪 测试指南

## ⚡ 快速开始

### 1. 重启服务器
```bash
npm start
```

### 2. 强制刷新浏览器
- **Windows/Linux**: `Ctrl + Shift + R`
- **macOS**: `Cmd + Shift + R`

### 3. 访问调试工具
打开浏览器访问: http://localhost:3000/debug.html

---

## 🔍 问题检查清单

### 问题1: 计时器不停止

**症状**: 视频生成完毕后，前端计时仍在继续

**检查步骤**:
1. 打开调试工具: http://localhost:3000/debug.html
2. 点击"检查版本"按钮
3. 确认版本为 `1.0.2`

**如果版本不对**:
1. 点击"清除缓存并刷新"
2. 重新打开主页面
3. 生成新视频测试

**验证修复**:
1. 生成一个新视频
2. 观察浏览器控制台（F12）应该显示：
   ```
   ✅ 视频生成完成！停止所有计时器...
   🛑 清除轮询计时器... ID=xxx
   🛑 清除生成计时器... ID=xxx
   ```
3. 计时器应该立即停止

---

### 问题2: 缺少生成报告和下载链接

**症状**: 视频生成完毕后，没有显示报告卡片和下载按钮

**检查步骤**:
1. 在调试工具中，输入视频ID
2. 点击"检查视频"
3. 确认 `generation_status` 为 `completed`
4. 确认 `video_filename` 有值

**测试报告**:
1. 在调试工具中，点击"测试显示报告"
2. 应该看到：
   - 视频时长
   - 文件大小
   - 视频格式
   - 下载按钮

**验证修复**:
1. 生成新视频
2. 完成后应该自动显示：
   ```
   📊 生成报告
   ⏱️ 视频时长: X秒
   📦 文件大小: X MB
   🎬 视频格式: MP4
   ⬇️ 下载视频 [按钮]
   ```

---

### 问题3: 列表状态不更新

**症状**: 视频列表中状态仍显示"生成中"

**检查步骤**:
1. 打开调试工具
2. 输入视频ID
3. 点击"检查视频"
4. 查看返回的 `generation_status`

**如果数据库状态正确，但前端显示不对**:
1. 切换到"视频列表" TAB
2. 观察控制台应该显示：
   ```
   🔄 刷新统计和列表...
   📋 刷新视频列表（当前在列表TAB）
   ```

**手动刷新**:
1. 在调试工具点击"刷新列表"
2. 返回主页面
3. 切换到"视频列表" TAB
4. 状态应该正确显示

---

### 问题4: 动画效果报错

**症状**: 选择动画后生成失败，后台显示 FFmpeg 错误

**最新修复** (提交 `f14124b`):
- ✅ 修复了所有滤镜语法错误
- ✅ 简化了滑动效果实现
- ✅ 正确转义了表达式中的逗号

**测试步骤**:
1. 选择不同的动画效果
2. 生成视频
3. 观察后台日志应该显示：
   ```
   🎬 应用动画效果: slide_right
     ✅ 从左向右滑入 (1.5秒)
   [FFmpeg] ... (正常执行)
   ✅ FFmpeg执行完成!
   ```

**支持的动画**:
- ✅ 无动画
- ✅ 淡入淡出
- ✅ 从右向左滑入
- ✅ 从左向右滑入
- ✅ 从下向上滑入
- ✅ 从上向下滑入
- ✅ 放大（0.5x -> 1.0x）
- ✅ 缩小（1.5x -> 1.0x）
- ✅ 旋转（360度）
- ✅ 弹跳

---

## 🐛 如果问题仍然存在

### 完整清除缓存步骤

1. **清除浏览器缓存**:
   - 按 `F12` 打开开发者工具
   - 右键点击刷新按钮
   - 选择"清空缓存并硬性重新加载"

2. **清除应用存储**:
   - 打开开发者工具（F12）
   - 切换到 "Application" 标签
   - 左侧选择 "Storage"
   - 点击 "Clear site data"

3. **使用隐私模式测试**:
   - 打开新的隐私/无痕窗口
   - 访问 http://localhost:3000
   - 测试所有功能

### 检查后台日志

确保后台日志显示：
```
🎬 [X] 开始生成视频...
🖼️  [X] 开始生成文本图像...
✅ [X] 文本图像生成成功
🎵 [X] 开始分析音频
✅ [X] 音频时长: X秒
⏱️  [X] 视频时长: X秒
🎬 [X] 开始合成视频...
🎬 应用动画效果: xxx
  ✅ [动画描述]
[FFmpeg] ... (正常执行)
✅ FFmpeg执行完成!
✅ [X] 视频生成完成
```

### 数据库检查

如果怀疑数据库有问题，运行：
```bash
psql -d slideshow_generator -c "SELECT id, generation_status, generation_progress, video_duration, video_filename FROM videos ORDER BY id DESC LIMIT 5;"
```

应该看到：
- `generation_status`: `completed`
- `generation_progress`: `100`
- `video_duration`: 正确的秒数（如 `5.00`）
- `video_filename`: 有文件名

---

## 📊 完整测试流程

### 测试用例1: 简单视频（无动画）

1. **输入文本**: "测试视频"
2. **上传音乐**: 任意音频文件
3. **时长**: 5秒
4. **动画**: 无动画
5. **格式**: MP4

**预期结果**:
- ✅ 生成时间: < 15秒
- ✅ 进度条: 0% -> 100%
- ✅ 计时器在完成时停止
- ✅ 显示生成报告
- ✅ 可下载视频

### 测试用例2: 淡入淡出动画

1. **动画**: 淡入淡出
2. 其他参数同上

**预期结果**:
- ✅ 视频开头淡入
- ✅ 视频结尾淡出
- ✅ 生成成功

### 测试用例3: 滑动动画

1. **动画**: 从右向左滑入
2. 其他参数同上

**预期结果**:
- ✅ 文本从右边滑入屏幕
- ✅ 后台日志显示正确的滤镜
- ✅ 无 FFmpeg 错误

### 测试用例4: 缩放动画

1. **动画**: 放大
2. 其他参数同上

**预期结果**:
- ✅ 文本从小放大
- ✅ 生成时间稍长（~30秒）
- ✅ 生成成功

---

## 🎯 关键指标

### 前端

| 指标 | 预期值 | 检查方法 |
|------|--------|----------|
| 版本号 | 1.0.2 | 调试工具 > 检查版本 |
| WebSocket连接 | ✅ 已连接 | 调试工具 > 检查连接 |
| 进度更新 | 每2秒 | 观察控制台日志 |
| 计时器 | 完成时停止 | 观察页面计时 |
| 报告显示 | 自动显示 | 完成后检查 |

### 后台

| 指标 | 预期值 | 检查方法 |
|------|--------|----------|
| FFmpeg版本 | 8.0+ | 后台日志首行 |
| 音频循环 | ✅ 支持 | 日志显示"循环播放" |
| 时长设置 | ✅ 生效 | 检查最终视频时长 |
| 动画效果 | ✅ 正常 | 无滤镜错误 |
| 状态更新 | ✅ 实时 | 轮询每2秒 |

---

## 📞 获取帮助

如果测试失败，请提供：

1. **调试工具截图**
2. **浏览器控制台日志**（完整的）
3. **后台服务器日志**（从开始生成到结束）
4. **视频ID**
5. **测试参数**（文本、时长、动画类型等）

将这些信息一起提供，我可以准确定位问题！

---

## ✅ 验证清单

完成测试后，确认以下所有项：

- [ ] 版本号为 1.0.2
- [ ] 进度条从 0% 更新到 100%
- [ ] 计时器在完成时立即停止
- [ ] 自动显示生成报告
- [ ] 显示下载按钮且可用
- [ ] 列表状态显示"已完成"
- [ ] 列表时长正确显示（如 0:05）
- [ ] 所有动画效果都能成功生成
- [ ] 幻灯片时长设置生效
- [ ] 音频能正确循环播放
