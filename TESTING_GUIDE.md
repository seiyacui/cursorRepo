# 🧪 测试指南

## 快速测试步骤

### 前提条件检查

```bash
# 1. 检查 Node.js
node -v
# 应显示: v14.x 或更高

# 2. 检查 PostgreSQL
psql --version
# 应显示: psql (PostgreSQL) 12.x 或更高

# 3. 检查 yt-dlp
yt-dlp --version
# 应显示版本号，如: 2023.xx.xx

# 4. 检查 PostgreSQL 是否运行
pg_isready
# 应显示: localhost:5432 - accepting connections
```

### 安装测试

```bash
# 运行安装脚本
./setup.sh

# 或手动安装
npm install
npm run init-db
```

### 启动测试

```bash
# 启动服务器
npm start

# 预期输出:
# ╔═══════════════════════════════════════════════════════╗
# ║   🎬 YouTube 视频批量下载器                            ║
# ║   服务器运行在: http://localhost:3000                ║
# ║   最大并发下载: 3                                      ║
# ╚═══════════════════════════════════════════════════════╝
```

## 功能测试清单

### 1. ✅ 前端界面测试

#### 页面加载
- [ ] 打开 http://localhost:3000
- [ ] 页面正常显示
- [ ] 统计数据显示（初始为 0）
- [ ] 所有表单控件正常显示

#### 表单功能
- [ ] 在输入框输入多行 URL
- [ ] 选择视频格式（MP4/MKV/WebM）
- [ ] 选择视频质量
- [ ] 勾选/取消 "同时下载音频"
- [ ] 选择音频格式（MP3/AAC/WAV/M4A）

### 2. ✅ 下载功能测试

#### 单个视频下载

测试 URL: `https://www.youtube.com/watch?v=jNQXAC9IVRw`

```
步骤:
1. 在输入框粘贴上述 URL
2. 选择 MP4 格式
3. 勾选下载音频，选择 MP3
4. 点击 "开始下载"
5. 观察进度区域是否显示
6. 检查进度条是否更新
7. 等待下载完成
8. 检查列表中是否出现新记录
9. 点击 "视频" 和 "音频" 按钮下载文件
```

预期结果:
- ✅ 进度实时更新
- ✅ 下载速度显示
- ✅ 完成后状态变为 "已完成"
- ✅ 文件可以下载
- ✅ 收到通知（如已配置）

#### 批量视频下载

测试 URLs:
```
https://www.youtube.com/watch?v=jNQXAC9IVRw
https://www.youtube.com/watch?v=dQw4w9WgXcQ
https://www.youtube.com/watch?v=9bZkp7q19f0
```

```
步骤:
1. 粘贴多个 URL（每行一个）
2. 配置下载选项
3. 点击 "开始下载"
4. 观察并发下载（最多3个同时）
5. 查看批次进度统计
6. 等待全部完成
7. 检查下载报告
```

预期结果:
- ✅ 最多 3 个视频同时下载
- ✅ 队列自动管理
- ✅ 总体进度正确显示
- ✅ 完成后显示下载报告
- ✅ 报告包含下载链接

### 3. ✅ 搜索功能测试

#### 关键字搜索
```
步骤:
1. 在搜索框输入关键字
2. 点击 "搜索" 按钮
3. 检查结果是否正确过滤
```

预期结果:
- ✅ 只显示匹配的视频
- ✅ 标题和文件名都被搜索

#### 日期范围搜索
```
步骤:
1. 选择开始日期
2. 选择结束日期
3. 点击 "搜索"
4. 检查结果时间范围
```

预期结果:
- ✅ 只显示日期范围内的视频
- ✅ 边界日期包含在内

#### 组合搜索
```
步骤:
1. 输入关键字
2. 选择日期范围
3. 点击 "搜索"
```

预期结果:
- ✅ 同时满足关键字和日期条件
- ✅ 搜索结果准确

#### 重置搜索
```
步骤:
1. 执行搜索
2. 点击 "重置" 按钮
```

预期结果:
- ✅ 搜索条件清空
- ✅ 显示所有视频

### 4. ✅ 导出功能测试

#### HTML 导出
```
步骤:
1. 点击 "导出 HTML" 按钮
2. 浏览器下载文件
3. 打开 HTML 文件
```

预期结果:
- ✅ 文件成功下载
- ✅ HTML 格式正确
- ✅ 中文显示正常
- ✅ 样式美观
- ✅ 数据完整

#### Markdown 导出
```
步骤:
1. 点击 "导出 Markdown" 按钮
2. 下载 .md 文件
3. 用文本编辑器打开
```

预期结果:
- ✅ Markdown 格式正确
- ✅ 表格显示正常
- ✅ 中文无乱码

#### PDF 导出
```
步骤:
1. 点击 "导出 PDF" 按钮
2. 下载 PDF 文件
3. 用 PDF 阅读器打开
```

预期结果:
- ✅ PDF 生成成功
- ✅ 排版美观
- ✅ 中文显示正常
- ✅ 可以打印

#### PNG 导出
```
步骤:
1. 点击 "导出 PNG" 按钮
2. 下载 PNG 图片
3. 查看图片
```

预期结果:
- ✅ 图片生成成功
- ✅ 清晰度良好
- ✅ 内容完整

### 5. ✅ WebSocket 实时通信测试

```
步骤:
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签
3. 开始下载视频
4. 观察 WebSocket 消息
```

预期日志:
```
✅ WebSocket 连接成功
收到消息: {type: 'batch_start', ...}
收到消息: {type: 'download_start', ...}
收到消息: {type: 'progress', ...}
收到消息: {type: 'download_complete', ...}
收到消息: {type: 'batch_complete', ...}
```

#### 断线重连测试
```
步骤:
1. 启动下载
2. 重启服务器
3. 观察前端是否自动重连
```

预期结果:
- ✅ 5秒后自动重连
- ✅ Console 显示重连消息
- ✅ 功能恢复正常

### 6. ✅ 通知功能测试

#### 配置通知
编辑 `.env` 文件:
```env
# 至少配置一个通知渠道
WXPUSHER_TOKEN=your_token
WXPUSHER_UID=your_uid
```

#### 测试通知
```
步骤:
1. 配置通知渠道
2. 重启服务器
3. 下载视频
4. 等待完成
5. 检查通知是否收到
```

预期结果:
- ✅ 下载完成后收到通知
- ✅ 通知包含下载统计
- ✅ 通知包含视频详情
- ✅ 4个渠道都能正常发送（如已配置）

### 7. ✅ 数据库测试

#### 连接测试
```bash
psql -U postgres -d youtube_downloader -c "SELECT COUNT(*) FROM videos;"
```

预期结果:
- ✅ 连接成功
- ✅ 显示视频数量

#### 数据完整性测试
```bash
psql -U postgres -d youtube_downloader -c "SELECT * FROM videos LIMIT 5;"
```

预期结果:
- ✅ 数据正确存储
- ✅ 中文字符正常
- ✅ 所有字段都有值

### 8. ✅ 并发下载测试

#### 修改并发数
编辑 `.env`:
```env
MAX_CONCURRENT_DOWNLOADS=5
```

```
步骤:
1. 重启服务器
2. 添加 10 个视频到队列
3. 观察同时下载数量
```

预期结果:
- ✅ 最多 5 个同时下载
- ✅ 其他视频在队列等待
- ✅ 完成后自动开始下一个

### 9. ✅ 错误处理测试

#### 无效 URL 测试
```
步骤:
1. 输入无效 URL: "not-a-url"
2. 点击下载
```

预期结果:
- ✅ 显示错误提示
- ✅ 状态标记为 "失败"
- ✅ 错误信息记录到数据库

#### 网络断开测试
```
步骤:
1. 开始下载
2. 断开网络
3. 观察错误处理
```

预期结果:
- ✅ 显示错误信息
- ✅ 不影响其他下载
- ✅ 可以重试

### 10. ✅ 删除功能测试

```
步骤:
1. 选择一个已下载的视频
2. 点击 "删除" 按钮
3. 确认删除
```

预期结果:
- ✅ 显示确认对话框
- ✅ 数据库记录删除
- ✅ 视频文件删除
- ✅ 音频文件删除
- ✅ 列表自动刷新
- ✅ 统计数据更新

## 性能测试

### 并发性能测试
```
测试场景: 下载 10 个视频
并发数: 3
预期时间: < 单个下载时间 × 10 / 3
```

### 数据库性能测试
```bash
# 插入 1000 条记录后测试查询速度
time psql -U postgres -d youtube_downloader -c "SELECT * FROM videos WHERE title ILIKE '%test%' ORDER BY created_at DESC LIMIT 100;"
```

预期结果:
- ✅ 查询时间 < 100ms
- ✅ 索引生效

## 浏览器兼容性测试

测试浏览器:
- [ ] Chrome (推荐)
- [ ] Firefox
- [ ] Safari
- [ ] Edge

预期结果:
- ✅ 所有主流浏览器正常工作
- ✅ WebSocket 支持正常
- ✅ 样式显示一致

## 移动端测试

```
步骤:
1. 用手机浏览器访问
2. 测试所有功能
```

预期结果:
- ✅ 响应式布局正常
- ✅ 触摸操作流畅
- ✅ 所有功能可用

## 压力测试

### 大批量下载测试
```
测试场景: 同时添加 50 个视频
```

预期结果:
- ✅ 队列管理正常
- ✅ 内存占用合理
- ✅ 不会崩溃
- ✅ 所有视频都能下载

### 长时间运行测试
```
测试场景: 连续运行 24 小时
```

预期结果:
- ✅ 无内存泄漏
- ✅ 无数据库连接泄漏
- ✅ WebSocket 连接稳定

## 常见问题排查

### 问题: 下载失败 "yt-dlp: command not found"
```bash
# 解决方案
brew install yt-dlp
# 或
pip3 install yt-dlp
```

### 问题: 数据库连接失败
```bash
# 检查 PostgreSQL 是否运行
brew services start postgresql

# 检查数据库是否存在
psql -U postgres -l | grep youtube_downloader
```

### 问题: WebSocket 连接失败
```
解决方案:
1. 检查防火墙设置
2. 确认端口 3000 未被占用
3. 查看浏览器控制台错误信息
```

### 问题: 通知发送失败
```
解决方案:
1. 检查 .env 配置是否正确
2. 测试网络连接
3. 查看服务器日志
```

### 问题: 中文乱码
```
解决方案:
1. 确认数据库编码为 UTF-8
2. 检查浏览器编码设置
3. 重新创建数据库: 
   CREATE DATABASE youtube_downloader WITH ENCODING 'UTF8';
```

## 测试报告模板

```
测试日期: ____________________
测试人员: ____________________
环境信息:
- OS: ____________________
- Node.js: ____________________
- PostgreSQL: ____________________
- yt-dlp: ____________________

功能测试结果:
- [ ] 前端界面: ✅ 通过 / ❌ 失败
- [ ] 下载功能: ✅ 通过 / ❌ 失败
- [ ] 搜索功能: ✅ 通过 / ❌ 失败
- [ ] 导出功能: ✅ 通过 / ❌ 失败
- [ ] 通知功能: ✅ 通过 / ❌ 失败
- [ ] 并发下载: ✅ 通过 / ❌ 失败
- [ ] 错误处理: ✅ 通过 / ❌ 失败
- [ ] 数据管理: ✅ 通过 / ❌ 失败

性能测试结果:
- 单视频下载速度: ____ MB/s
- 并发下载效率: ____ 倍提升
- 数据库查询速度: ____ ms
- 内存占用: ____ MB

问题记录:
1. ______________________________
2. ______________________________
3. ______________________________

总体评价:
□ 优秀  □ 良好  □ 合格  □ 需改进

备注:
_________________________________
_________________________________
```

## 自动化测试脚本

创建 `test.sh`:
```bash
#!/bin/bash

echo "🧪 开始自动化测试..."

# 1. 检查依赖
echo "1. 检查依赖..."
command -v node >/dev/null 2>&1 || { echo "❌ Node.js 未安装"; exit 1; }
command -v psql >/dev/null 2>&1 || { echo "❌ PostgreSQL 未安装"; exit 1; }
command -v yt-dlp >/dev/null 2>&1 || { echo "❌ yt-dlp 未安装"; exit 1; }
echo "✅ 依赖检查通过"

# 2. 启动服务器（后台）
echo "2. 启动服务器..."
npm start > test.log 2>&1 &
SERVER_PID=$!
sleep 5

# 3. 健康检查
echo "3. 健康检查..."
curl -s http://localhost:3000/health | grep -q "ok" && echo "✅ 服务器运行正常" || echo "❌ 服务器启动失败"

# 4. API 测试
echo "4. API 测试..."
curl -s http://localhost:3000/api/statistics | grep -q "success" && echo "✅ API 正常" || echo "❌ API 失败"

# 5. 清理
echo "5. 清理..."
kill $SERVER_PID

echo "✅ 测试完成"
```

运行测试:
```bash
chmod +x test.sh
./test.sh
```

---

**测试完成后，请填写测试报告并报告任何问题！** 🎉
